{
  "name": "gs-finance",
  "version": "1.0.0",
  "description": "GS Finance — dashboard de controle de gastos (Fintech FIAP). HTML + Tailwind CSS.",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
